import 'package:flutter/material.dart';

class JobViewCubit extends StatelessWidget {
  const JobViewCubit({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}