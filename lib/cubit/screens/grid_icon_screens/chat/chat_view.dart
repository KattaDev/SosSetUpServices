import 'package:flutter/material.dart';

class ChatViewCubit extends StatelessWidget {
  const ChatViewCubit({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}