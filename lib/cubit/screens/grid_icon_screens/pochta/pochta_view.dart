import 'package:flutter/material.dart';

class EmailViewCubit extends StatelessWidget {
  const EmailViewCubit({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}